Save your models here. You can use "pickle" or sci-kit learn's "joblib" to dump the bytes of your model to this folder.

randomforest100.pkl - Random Forest Classifier with 100 trees, using entropy with max depth of 10 (0.778)

linearsvm.pkl - SVM with linear kernel w C=1e-07, and hinge loss (0.743)

rbfsvm.pkl - Bagged SVM with rbf kernel, using 20 estimators each trained on 10% of the data and features (0.744)
 
randomforest1000.pkl - Random Forest with 1000 trees, using entropy and max depth of 20 (0.781)
